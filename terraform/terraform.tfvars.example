# Copy to terraform.tfvars locally; never commit credentials or state.
aws_region     = "us-east-1"
environment    = "dev"
project_name   = "nyc-hvfhs-lakehouse"
s3_bucket_name = "<globally-unique-private-bucket>"
